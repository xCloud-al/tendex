/**
 * evaluation service
 */

import { factories } from '@strapi/strapi';
import {createPartFromUri, createUserContent, GoogleGenAI} from "@google/genai";

export default factories.createCoreService('api::evaluation.evaluation', ({ strapi }) => ({


}));
